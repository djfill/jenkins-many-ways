Jenkins on local VirtualBox using Vagrant

Prerequisites:
Install VirtualBox
Install Vagrant

ensure you are in the vagrant-virtualbox directory

Start Jenkins box
vagrant up

SSH Access
vagrant ssh

Jenkins Access
http:localhost:8080

Remove Jenkins box
vagrant destroy
